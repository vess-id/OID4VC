name: Claude auto PR review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  claude-review:
    uses: cvoxelprotocol/.github/.github/workflows/reuse-claude-pr-review.yml@main
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
